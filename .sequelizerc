const path = require("path");

module.exports = {
  config: path.resolve("config", "config.json"), // Adjust the path to your database configuration file
  "models-path": path.resolve("models"), // Adjust the path to your models directory
  "migrations-path": path.resolve("migrations"), // Adjust the path to your migrations directory
};
